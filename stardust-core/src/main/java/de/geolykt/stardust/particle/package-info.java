/**
 * Particles are jars that are loaded into memory by Stardust and then run.
 * They either are responsible for actually modifing the game's behaviour or
 * at the very least are responsible for 
 */
package de.geolykt.stardust.particle;
